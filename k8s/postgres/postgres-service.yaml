apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: health-monitoring
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
      protocol: TCP
  selector:
    app.kubernetes.io/name: postgres
