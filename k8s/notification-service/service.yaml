apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: health-monitoring
spec:
  selector:
    app: notification-service
  ports:
  - port: 8003
    targetPort: 8003
  type: ClusterIP