apiVersion: v1
kind: Service
metadata:
  name: monitoring-dashboard
  namespace: health-monitoring
spec:
  selector:
    app: monitoring-dashboard
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP